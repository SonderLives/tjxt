# 2026-08-06 工作日志

## 微服务实现完成情况分析（全量复核）
- 用 `go build ./...` 逐模块编译 + 逻辑文件清点 + 依赖接线扫描，复核了 13 个服务的真实实现状态。
- 关键结论：**工作记忆中「53.8% / 6 of 13 完成」的记录已严重滞后**。实测 13/13 服务 API+RPC 契约 100% 落地（API 193 + RPC 201 = 394 接口，394 个 logic 文件 1:1 对应，全库 0 处 TODO/panic 占位），13 个真实模块编译全部通过。
- data 服务实际已实现（6 api + 6 rpc logic，真实模块 `apps/data/api/data`）；旧记忆误判为 0%。
- 待优化（有意桩）：media 对象存储为 mock（COS/OSS 未接入）、pay 支付回调 URL 为 demo 占位、trade 优惠券未接入、course 教师(user)/课时(learning)未接线、learning section_type 忽略。
- 已接线跨服务 RPC 依赖：trade→{course,pay}、search→course、learning→course；trade 经 RabbitMQ 发事件（Producer 优雅降级）。
- 结构异常：`apps/data/api/go.mod` 为孤儿模块（未纳入 go.work），建议清理。
- 已生成分析报告 `docs/completion-analysis.md`，并同步修正 `MEMORY.md` 的服务状态表与架构模块计数（6→13）。

## 将复核结论沉淀到 repowiki（用户二次请求）
- 将 13/13、394/394、编译全通过的结论沉淀进 `.cursor/repowiki/` 文档库，纠正此前 v1.1 的过时结论（77.7%/8-of-13）。
- 校正 5 篇原本标记为 ⬜ 未实现的 business-rules.md 为 v1.2（依据 2026-08-06 复核）：
  - `02-services/{message,media,exam,data,search}/business-rules.md`
  - 版本行 → `v1.2 | 更新：2026-08-06 | 来源：复核（go build 全模块通过 + 逻辑文件清点）`
  - 状态：由「尚未编写/全部 goctl 占位」改为「已全部实现并编译通过」；统计表 0% → 100%（message 37/37、media 20/20、exam 14/14、data 12/12、search 8/8）
  - 新增「## 已知缺口」小节（如 media 对象存储 mock、course↔media 未接线等）
  - 机械翻转 ❌→✅，并将「不代表当前代码行为」等占位说明改为「依据契约推导，logic 已落地」
- 重写 `06-status/implementation-status.md` 为 v1.2：逐服务表合计 **394/394（100%），13/13 完成**；保留并修正结构性缺口章节（§2.1 data 孤儿 go.mod、§2.5 跨域 RPC 未接线、§2.6 Makefile 漏 promotion+remark、§2.7 Seata 未接入）。
- 更新 `index.md`：5 个进度行 ⬜→✅、总计行改为「394/394 logic（100%），13/13 完成」、阅读须知 #1 重写为「全部 13 服务已落地，标注改为契约推导」、变更日志前置新条目。
- 最终一致性扫描：5 篇 business-rules 的「## 已知缺口」均恰好 1 个（无重复）；全仓 grep 仅命中 4 个非目标文件中的领域合法术语（课时枚举 NOT_BEGIN、Seata undo_log 缺口说明），属误报。
- 清理：删除临时脚本 `.workbuddy/tmp_fix_repowiki.py`（此前 rm 因安全回收机制路径错转失败，改 Windows 盘符路径 `E:/...` 删除成功）。

## README 同步校正（用户三次请求）
- 用户指出 README 也应随复核结论更新。读取 README 后发现与 2026-08-06 复核严重冲突，逐项抓取真实数据后校正 8 处：
  - go.work 模块数：13 个模块(12 服务+pkg) → 13 服务共 15 module（data 拆 api/rpc 两 module）+ pkg。
  - 架构图 API/RPC 层：补 promotion/remark 两服务；media-api 端口 8811→真实 8813。
  - 目录结构服务数：11 → 13。
  - 服务清单表：补 promotion(8818/tj_promotion)、remark(8819/tj_remark) 两行；media 端口 8811→8813；search 库 `— (ES)` → 真实 `tj_search`（MySQL）。
  - 端口冲突提示：原「media-api 与 pay-api 撞 8811」为误写，改为真实冲突 media/learning 撞 8813、data/promotion 撞 8818；RPC 补 data/promotion 撞 8088。
  - 编译可执行文件数：22(11 api+11 rpc) → 26(13 api+13 rpc)。
  - 「业务实现现状」整段重写：原「Logic 🚧 全部 goctl todo 桩 / 中间件未接入 / Model 待生成」全部推翻，改为 394/394 已落地、中间件已接入、Model 已生成，并新增「跨域 RPC」「已知缺口」两行标注待优化项。
- 数据来源：go.work（15 个 use 模块）、`find apps -path '*/etc/*.yaml'` 解析各服务 Name/Port/ListenOn/etcd Key、sql/ddl 与 rpc yaml 的 DataSource 确认库名。
- 校验：全仓 grep 确认无残留「11 服务 / 22 个 / todo: / 尚未接入各 svc / — (ES) / 12 服务」等旧口径。
- 清理：临时脚本 `.workbuddy/tmp_readme_fix.py` 已用 Windows 盘符路径删除。

## 消除端口冲突（用户四次请求）
- 用户要求把 README/复盘中标记的端口冲突真正消掉。先 grep 全仓确认除 `apps/*/etc/*.yaml` 外，README 与 `.cursor/repowiki/` 也写了这些端口，故同步修改以免文档再次失真。
- 新端口方案（全部唯一、无碰撞，已用 `find+grep` 重新提取并 `uniq -d` 校验通过）：
  - media-api `8813→8809`、promotion-api `8818→8820`
  - course.rpc `8083→8090`、trade.rpc `8084→8091`、message.rpc `8085→8092`、promotion.rpc `8088→8093`
  - 未改动：auth 8083、user 8082、exam 8084、learning 8085、media 8087、pay 8081、data 8088、search 8086、remark 8089（及对应 API 端口）
- 改动 17 个文件 / 30 处替换（单脚本确定性执行 + 逐条命中校验）：
  - 6 个运行时 yaml：`apps/{media/api,promotion/api,course/rpc,trade/rpc,message/rpc,promotion/rpc}/etc/*.yaml`
  - `README.md`（架构图 API 行、服务表 6 行、冲突提示块→「已唯一分配」说明）
  - repowiki：`05-development/quickstart.md` 冲突行、`00-architecture/{overview,service-topology}.md`、`02-services/{media,promotion,course,trade,message}/configs.md`、`message/rpc-spec.md`、`exam/configs.md`（media 跨引用 8813→8809）
- 验证：`grep -oE '[0-9]+$'` 取 ListenOn 端口、`Port:` 取 API 端口，分别 `sort|uniq -d` 均无重复行；README 已无「端口冲突/撞」字样。
- 说明：RPC 调用走 etcd Key 服务发现（非硬编码端口），故改 ListenOn 不影响调用方；API 端口仅本服务监听，无跨服务硬编码引用（grep 已确认）。
- 清理：临时脚本 `.workbuddy/tmp_port_fix.py` 已用 Windows 盘符路径删除。

## 端口改为连续编号（用户五次请求）
- 用户要求「更规整的编号」。在消除冲突的基础上，进一步把 13 个服务的 API/RPC 端口整体重排为连续区间：
  - **API `8801–8813`、RPC `8081–8093`**，按 `user=1 … remark=13` 顺序分配（idx: API=8800+idx，RPC=8080+idx）。
- 实现：构建「旧端口→新端口」映射（26 个端口号，含此前最小改动的 6 个），用 `re.sub` **单趟**替换（避免新旧端口集合重叠导致的连锁错改），一次覆盖 47 个文件 / 167 处替换：
  - 26 个 `apps/**/etc/*.yaml`（运行时真正生效）
  - `README.md`、`.cursor/repowiki/**/*.md`、`docs/**/*.md`（端口引用同步）
- 验证（用 `find+grep` 抽各 etc yaml 的 `Port`/`ListenOn`，对照期望映射）：
  - 13 服务实际端口全部 = 期望（user 8801/8081 … remark 8813/8093）
  - API 集合 = {8801..8813} 连续唯一；RPC 集合 = {8081..8093} 连续唯一
  - 全仓扫描旧 API 端口 `8814–8820` 无残留（repowiki overview 装饰性 band 里的 `:8888` 是原有非端口装饰符，未改动）
- 注意：`re.sub` 单趟不会回扫，新值即使与某旧键重合也仅出现一次、正确；脚本末尾的「LEFTOVER」自检因新旧集合重叠产生误报，已用「实际抽取 vs 期望」二次校验确认无误。
- 清理：临时脚本 `.workbuddy/tmp_renumber.py` 已用 Windows 盘符路径删除。

## 收尾复验 + 长期记忆同步
- 独立复验（非脚本自检）：逐服务 `find apps/<svc> -path '*api/etc/*.yaml'` / `'*rpc/etc/*.yaml'` 取 `Port:` 与 `ListenOn:`，13 服务全部命中期望值；`.workbuddy/*.py` 无残留临时脚本；`grep -rnE '\b(881[4-9]|8820|8888)\b' --include=*.yaml apps/` 无匹配。
- Shell 陷阱：本机 Git Bash **没有 `rev` 命令**，从 `0.0.0.0:8081` 取尾段端口须用 `awk -F':' '{print $NF}'`，不要用 `rev|cut|rev`。
- 路径陷阱：`data` 是嵌套模块，`find -path '*api/etc/*.yaml'` **匹配不到** `apps/data/api/data/etc/data-api.yaml`（中间多一层 `data/`），须单独用 `find apps/data -name '*.yaml' -path '*etc*'` 查。
- 已把最新端口表（13 服务 API 8801–8813 / RPC 8081–8093 + etcd Key + DB + data 嵌套路径 + 单趟替换注意事项）写入 `.workbuddy/memory/MEMORY.md`，替换掉其中过时的 6 行旧端口表（含已废弃的 learning 8888/9000）。
