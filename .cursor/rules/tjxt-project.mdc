---
description: 天机学堂 tjxt 项目核心约定（go-zero 微服务、goctl 生成优先、统一响应/分页/JWT）
alwaysApply: true
---

# tjxt 项目核心约定

本规则适用于整个仓库，任何代码生成、修改、重构都必须遵守。

## 架构与模块

- go-zero v1.10.3 微服务，`go.work` 工作区管理，每个服务模块路径为 `module tjxt/apps/<svc>`（rpc 子目录无独立 go.mod）。
- 公共库在 `pkg/`（模块名 `tjxt/pkg`），各服务通过 `replace tjxt/pkg => ../../pkg` 引用；复用工具一律放 `pkg`，禁止在服务内复制粘贴。
- 服务拆分：`apps/<svc>/api`（HTTP 网关层）+ `apps/<svc>/rpc`（业务/数据层）。跨服务调用只能走 RPC client，禁止跨服务 import internal 包或直接连别人的库。
- 命名风格统一 `gozero`（无下划线小驼峰目录/文件名），与 goctl 默认一致。

## goctl 生成优先（强约束）

- 开发顺序固定：DB 表 → Model(goctl) → Proto → RPC 生成 → .api → API 生成 → 手写 Logic。
- 只手写 `internal/logic`、custom model（`xxxmodel.go`）和业务扩展；**禁止手写** handler/types/routes/pb/server 骨架。
- Model 已存在时在 custom model 文件里扩展方法，不要重新生成覆盖 `*_gen.go`。
- `.api` 语法不支持 `any`，用 `interface{}` 代替。
- 标准命令（均已验证，可直接建议给用户）：
  - API: `cd apps/X/api && goctl api go --api X.api --dir . --style gozero`
  - RPC: `cd apps/X/rpc && goctl rpc protoc X.proto --go_out=. --go-grpc_out=. --zrpc_out=. --style gozero`
  - Model: `goctl model mysql ddl -src sql/ddl/tj_X.sql -dir apps/X/rpc/internal/model -cache --style gozero`
- 全量重新生成用 `make api` / `make rpc` / `make generate`；goctl 不会覆盖已有 handler/logic/svc/config，只刷新 types/routes/pb/server。

## 统一响应与错误

- API handler 返回统一用 `result.Write(w, r, data, err)`（`pkg/response`，`R{Code,Msg,RequestId,Data any}`），**不使用** goctl 生成的 `httpx.OkJsonCtx` / Result 类型。
- 业务错误码集中在 `pkg/xerr/code.go`，新增错误码放这里，logic 返回 `xerr` 包装的错误。
- 分层职责：handler 只做参数解析与响应写回；业务判断、DB、RPC 调用全部在 logic。

## 统一分页

- 请求用 `PageRequest{PageNo, PageSize}`（`pkg/utils/page`），响应用对应分页结构；不要各自发明分页字段。

## 认证

- 需要登录的接口在 `.api` 的 `@server` 上加 `jwt: Auth`；从 context 取用户身份用 `pkg/auth`，不要在 handler 里手写 JWT 解析。

## 数据与 SQL

- 纯 DDL 放 `sql/ddl/<domain>.sql`（供 goctl model 使用），含初始数据的迁移脚本放 `sql/migration/`。
- 每个服务只访问自己的库 `<domain>`；跨域数据通过对应服务的 RPC 获取。

---

> 📚 详细文档见 `.cursor/repowiki/`：架构总览 `00-architecture/overview.md`、开发规范 `01-conventions/`、服务级规格 `02-services/<svc>/`（api-spec/rpc-spec/data-model/business-rules/configs）、OpenAPI 契约 `docs/tjxt.openapi.json`。
